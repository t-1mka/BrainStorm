services:
  - type: web
    name: brainstorm
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn -w 4 -k eventlet -b 0.0.0.0:$PORT wsgi:app
    envVars:
      - key: PORT
        value: 10000
      - key: PYTHON_VERSION
        value: 3.11
